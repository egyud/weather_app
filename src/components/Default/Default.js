import React from 'react';
import classes from './Default.module.css';

const Default = () => (
  <div className={classes.Default}>
    <h1>Weather App Name Placeholder</h1>
    <p>Enter your zip code above for current weather and a 7 day forecast.</p>
  </div>
);

export default Default;