import React from 'react';
import classes from './Default.module.css';

const Default = () => (
  <div className={classes.Default}>
    <h1>Enter your zip code above for current weather and a 7 day forecast.</h1>
  </div>
);

export default Default;